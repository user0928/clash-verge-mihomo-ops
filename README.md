# clash-verge-mihomo-ops

Windows Clash Verge Rev + mihomo operations skill. It captures the previously verified selective-routing workflow: reconnect 5/5 diagnosis, TUN/网卡 baseline, OpenAI/Codex routing, and minimal application-specific repairs for Google, XMind, Bilibili, and WeChat.

The skill intentionally contains no subscription URL, node list, controller secret, or generated full profile. Discover those values on the target machine, back up the persistent source, apply the smallest change, restart/reapply, and verify the generated runtime configuration.

Install by copying this directory into the Codex skills directory, or use the GitHub repository's skill installer path once the repository is available.
